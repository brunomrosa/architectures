import styled from 'styled-components';

const Container = styled.button`
  min-width: 200px;
  min-height: 50px;

  background-color: var(--secondary);

  color: var(--text);
  border: none;
  border-radius: 5px;
`;

export {Container}